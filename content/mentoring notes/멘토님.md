# DB 관련
1. JOIN의 종류와 동작 원리
	1. Inner join
	2. left join
	3. right join left
	4. full outer join
	5. cross join
	- 어떨게 사용되는지, 상황에 따라 어떻게 쓰이는지
	- 어떤 상황을 주고 쿼리를 직접 짜라고 함..! 어떤 도메인 모델에 대해서 디자인해보라고 하고, 테이블 보고 실제 상황 가정하면서 join query 작성해보라
2. SELECT 쿼리 동작 순서
	- FROM/JOIN
	- WHERE
	- GROUP BY
	- HAVING
	- SELECT
	- DISTINCT
	- ORDER BY
	- LIMIT / OFFSET
	- 실제로 면접에서 많이 나옴..!
	- 외우기보단 이 순서를 보면 그림으로 그릴 수 있게 됨 >>
	- 한단계 더: 성능을 올리려면 WHERE 조건에 인덱스를 활용하는게 핵심
3. 관계형 DB vs NoSQL
	- 왜 선택했는지 완전 명확하게 설명!!!!!!
		- 프로젝트에 어떻게 더 적합
		- mongodb를 고른 이유 -> 데이터가 저장되는 구조가 비정형이여서! 
4. 샤딩 VS 파티셔닝
	- 물리적 vs 논리적
	- Partitioning
		- 동일한 DB에서 테이블을 분리해서 저장
	- Sharding
		- 분산처리를 하기 위해 DB 서버 자체를 분리     
	- DB 트래픽이 엄청 몰릴떄 어떡할꺼냐 등등
		- 이런 질문에 미리미리 대비
	- 기억해야할때
		- 내가 해왔던 경험 기반으로 무조건!!!!
			- 그냥 무작정 외우면 떨어진다
		- 만약에 x를 (x가 없는 프로젝트에) 도입한다면 이렇게 이렇게 하겠다~~ << 그 회사에 맞는 회사에 맞춰서 재구성하기!!!
		- 내가 가진 경험을 각색을!
5. 트랜색션과 Isolation level
	- 특히 isolation level!!!
		- 실제 isolation 레벨별로 실무에서 처리해본 경험이 있느냐...........?
	- Propagation!!!
		- 언제 어떻게 써봤는지!!!!!
6. 인덱스 & 성능 최적화
	- 쿼리에 대한 실행계획...?
		- 어떻게 어떤 인덱스가 사용되고 조회되는지...?<<<
	- B Tree 인덱스
	- Hash 인덱스
7. 정규화
	- 성능을 업데이트 vs 관리에중점 < 이 두가지에 따라서 나뉜다
8. Deadlock
	- DB 락이랑 다른거임
9. DB lock 종류
10. OLTP vs OLAP 차이
11. Replication 
	- Master slave
	- Multi-Master
	- 어떤 상황해서 뭐가 유리한지?
12. N + 1
	- 사례  -> 어떻게 해결
	- 실제로 운영환경에서는 겪어본적이 없었지만, 제가 해본 프로젝트에서는 어쩌구저쩌구


- 뉴스
	- paging 처리
	- redis
- 자료구조
	- 이 질문 너무 많이 한다? 그럼 면졉관이 물어볼게 없다는거임...ㅋㅋ
	- 알고리즘 푼 문제를 설명하는 연습을 해야한다.
		- 코딩테스트 -> 만약에 개선해볼 수 있으면 어떻게 개선해볼 수 있겠어??